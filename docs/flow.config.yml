# /flow settings (shared, committed)
language: ja
commands:
  test: pnpm test
  lint: pnpm lint
  typecheck: pnpm typecheck
ticket:
  tracker: github
  prefix: T
  pad: 6
repository:
  host: github
  default_branch: main
review:
  required: false
gates: [approach, plan, pr]
